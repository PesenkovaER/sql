# Лабораторная работа №4  
## Оконные функции для анализа данных  

**Номер варианта:** 15  

---

# Цель работы

Изучить концепцию оконных функций в SQL и научиться применять их для выполнения сложных аналитических расчетов: ранжирования, вычисления скользящих средних, нарастающих итогов и сравнительного анализа строк без группировки данных.

---

# Задачи

- Разобрать синтаксис оконных функций: `OVER`, `PARTITION BY`, `ORDER BY`, `ROWS` / `RANGE`.
- Научиться использовать функции ранжирования: `ROW_NUMBER()`, `RANK()`, `DENSE_RANK()`, `NTILE()`.
- Освоить функции смещения: `LAG()`, `LEAD()`.
- Применить агрегатные функции в качестве оконных для вычисления накопительных итогов и скользящих статистик.

---

# Индивидуальные задания

## Задание 1  
Найти 5 самых последних зарегистрированных клиентов в каждом штате  
(использовать `ROW_NUMBER()` по `date_added`).

<img width="723" height="709" alt="image" src="https://github.com/user-attachments/assets/67885141-a62e-4208-8d86-ee943bbb1130" />

Скриншот выполнения запроса (ROW_NUMBER, PARTITION BY state)

---

## Задание 2  
Разделить продажи на 5 групп (`NTILE`) по величине суммы сделки.

<img width="375" height="517" alt="image" src="https://github.com/user-attachments/assets/450b9bee-c0ce-48a1-a0db-03d5eb98a05c" />

Скриншот выполнения запроса (NTILE)

---

## Задание 3  
Вычислить скользящее среднее продаж за 10 последних транзакций  
для дилера с `dealership_id = 1`.

<img width="636" height="530" alt="image" src="https://github.com/user-attachments/assets/bbfd0045-b8a0-4aa3-9e1e-77c4936abc2c" />

Скриншот выполнения запроса (AVG OVER + ROWS BETWEEN)

---

# Вывод

В ходе выполнения лабораторной работы были реализованы аналитические запросы с использованием оконных функций SQL.

Было выполнено:

- Определение пяти последних зарегистрированных клиентов в каждом штате с применением функции `ROW_NUMBER()` и разбиением данных по штатам (`PARTITION BY state`)
- Разделение всех продаж на пять равных групп по величине суммы сделки с использованием функции `NTILE(5)`
- Расчёт скользящего среднего значения продаж по десяти последним транзакциям для дилера с `dealership_id = 1` с использованием оконной агрегатной функции `AVG()` и рамки `ROWS BETWEEN 9 PRECEDING AND CURRENT ROW`.
